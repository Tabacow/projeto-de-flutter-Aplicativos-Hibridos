class Raid {
  var id;
  var hostFC;
  var pokeNumber;
  var pokeName;
	var pokeType1;
	var pokeType2;

  Raid(this.id, this.hostFC, this.pokeNumber, this.pokeName, this.pokeType1, this.pokeType2);
  //public PokemonDTO pokemon;
}